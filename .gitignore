# Rust build artifacts
bot/target/
target/

# SQLite databases
*.sqlite
*.sqlite3
bot/discord-bot.sqlite3

# Environment files
.env
.env.prod
.env.local
.env.wapp
.env.telegram
config.json

# Config directory (runtime data, not source)
config/

# Docker
docker-compose.yml.bak

# Audios (runtime generated)
audios/
bot/audios

# WhatsApp session credentials (runtime generated by the bridge)
wapp-bridge/auth_state/

# IDE
.vscode/

# Aider
.aider*

# PocketTTS voice-cloning model files (194MB, baked into the sidecar image)
models/
